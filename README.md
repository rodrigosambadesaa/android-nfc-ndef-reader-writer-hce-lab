# NFC NDEF reader/writer + HCE lab
Reader Mode para leer tags, escritura de un registro NDEF de texto y un `HostApduService` con AID educativo `F0010203040506`. La escritura modifica físicamente el tag: usa etiquetas propias y regrabables.
